descriptor_params:
  name: hndesc
  backbone: caps
  exper_settings_name: ${descriptor.descriptor_params.name}_${descriptor.descriptor_params.backbone}_MP_st
  snapshot: ${paths.snapshots_dir}/${descriptor.descriptor_params.name}/${descriptor.descriptor_params.exper_settings_name}.pth
